<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Aqool Wire - Intelligence Platform</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --bg-dark: #0a1628;
            --bg-base: #0f1e2e;
            --bg-light: #1a2942;
            --green-bright: #00ff41;
            --green: #00e436;
            --cyan: #00d9c8;
            --cyan-bright: #00ffd9;
            --blue: #0099ff;
            --blue-bright: #00b4ff;
            --white: #ffffff;
            --gray-light: #e2e8f0;
            --gray-medium: #94a3b8;
            --gray-dark: #475569;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: var(--bg-dark);
            color: var(--white);
            line-height: 1.6;
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
            position: relative;
        }

        h1, h2, h3 {
            font-weight: 700;
            line-height: 1.2;
        }

        /* Animated Vector Grid Background */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                linear-gradient(rgba(0, 255, 65, 0.04) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0, 255, 65, 0.04) 1px, transparent 1px),
                linear-gradient(rgba(0, 217, 200, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0, 180, 255, 0.03) 1px, transparent 1px);
            background-size: 100px 100px, 100px 100px, 20px 20px, 20px 20px;
            background-position: 0 0, 0 0, 0 0, 0 0;
            pointer-events: none;
            z-index: 0;
            animation: grid-move 20s linear infinite;
            opacity: 0.5;
        }

        @keyframes grid-move {
            0% {
                background-position: 0 0, 0 0, 0 0, 0 0;
            }
            100% {
                background-position: 100px 100px, 100px 100px, 20px 20px, 20px 20px;
            }
        }

        /* Gradient Overlay */
        body::after {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 30%, rgba(0, 255, 65, 0.1) 0%, transparent 40%),
                radial-gradient(circle at 80% 70%, rgba(0, 153, 255, 0.1) 0%, transparent 40%),
                radial-gradient(circle at 50% 50%, rgba(0, 217, 200, 0.08) 0%, transparent 50%);
            pointer-events: none;
            z-index: 0;
        }

        /* Logo */
        .logo {
            width: 60px;
            height: 60px;
            position: relative;
            display: inline-block;
            margin-bottom: 30px;
        }

        .logo-rays {
            position: absolute;
            width: 100%;
            height: 100%;
        }

        .logo-ray {
            position: absolute;
            width: 3px;
            height: 24px;
            left: 50%;
            top: 0;
            transform-origin: 50% 30px;
            border-radius: 2px;
        }

        .logo-ray:nth-child(1) { transform: rotate(0deg); background: linear-gradient(to bottom, #00ff41, #00d9c8); }
        .logo-ray:nth-child(2) { transform: rotate(15deg); background: linear-gradient(to bottom, #00ff41, #00d9c8); }
        .logo-ray:nth-child(3) { transform: rotate(30deg); background: linear-gradient(to bottom, #00ff41, #00d9c8); }
        .logo-ray:nth-child(4) { transform: rotate(45deg); background: linear-gradient(to bottom, #00f055, #00d9c8); }
        .logo-ray:nth-child(5) { transform: rotate(60deg); background: linear-gradient(to bottom, #00e869, #00d9c8); }
        .logo-ray:nth-child(6) { transform: rotate(75deg); background: linear-gradient(to bottom, #00df7d, #00d9c8); }
        .logo-ray:nth-child(7) { transform: rotate(90deg); background: linear-gradient(to bottom, #00d991, #00d9c8); }
        .logo-ray:nth-child(8) { transform: rotate(105deg); background: linear-gradient(to bottom, #00d9a5, #00d9c8); }
        .logo-ray:nth-child(9) { transform: rotate(120deg); background: linear-gradient(to bottom, #00d9b9, #00d9c8); }
        .logo-ray:nth-child(10) { transform: rotate(135deg); background: linear-gradient(to bottom, #00d9c8, #00d9c8); }
        .logo-ray:nth-child(11) { transform: rotate(150deg); background: linear-gradient(to bottom, #00d9c8, #00cce6); }
        .logo-ray:nth-child(12) { transform: rotate(165deg); background: linear-gradient(to bottom, #00d9c8, #00b4ff); }
        .logo-ray:nth-child(13) { transform: rotate(180deg); background: linear-gradient(to bottom, #00d9c8, #00a0ff); }
        .logo-ray:nth-child(14) { transform: rotate(195deg); background: linear-gradient(to bottom, #00d9c8, #0099ff); }
        .logo-ray:nth-child(15) { transform: rotate(210deg); background: linear-gradient(to bottom, #00d9c8, #0099ff); }
        .logo-ray:nth-child(16) { transform: rotate(225deg); background: linear-gradient(to bottom, #00d9c8, #0099ff); }
        .logo-ray:nth-child(17) { transform: rotate(240deg); background: linear-gradient(to bottom, #00d9c8, #00a0ff); }
        .logo-ray:nth-child(18) { transform: rotate(255deg); background: linear-gradient(to bottom, #00d9c8, #00b4ff); }
        .logo-ray:nth-child(19) { transform: rotate(270deg); background: linear-gradient(to bottom, #00d9c8, #00d9c8); }
        .logo-ray:nth-child(20) { transform: rotate(285deg); background: linear-gradient(to bottom, #00d9c8, #00d991); }
        .logo-ray:nth-child(21) { transform: rotate(300deg); background: linear-gradient(to bottom, #00d9c8, #00df7d); }
        .logo-ray:nth-child(22) { transform: rotate(315deg); background: linear-gradient(to bottom, #00d9c8, #00e869); }
        .logo-ray:nth-child(23) { transform: rotate(330deg); background: linear-gradient(to bottom, #00d9c8, #00f055); }
        .logo-ray:nth-child(24) { transform: rotate(345deg); background: linear-gradient(to bottom, #00d9c8, #00ff41); }

        .logo-center {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 18px;
            height: 18px;
            background: radial-gradient(circle, var(--cyan-bright), var(--cyan));
            border-radius: 50%;
            box-shadow: 0 0 20px var(--cyan), 0 0 40px rgba(0, 217, 200, 0.5);
        }

        /* Sections */
        section {
            position: relative;
            padding: 100px 30px;
            max-width: 1200px;
            margin: 0 auto;
            z-index: 1;
        }

        /* Badge */
        .badge {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            padding: 12px 24px;
            background: rgba(0, 217, 200, 0.1);
            border: 1px solid rgba(0, 217, 200, 0.3);
            border-radius: 50px;
            font-size: 0.9rem;
            font-weight: 600;
            color: var(--cyan-bright);
            margin-bottom: 40px;
            letter-spacing: 0.02em;
        }

        .badge-dot {
            width: 8px;
            height: 8px;
            background: var(--cyan-bright);
            border-radius: 50%;
            box-shadow: 0 0 10px var(--cyan-bright);
            animation: pulse-dot 2s ease-in-out infinite;
        }

        @keyframes pulse-dot {
            0%, 100% { opacity: 1; transform: scale(1); }
            50% { opacity: 0.7; transform: scale(1.2); }
        }

        /* Hero Section */
        #hero {
            min-height: 90vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 120px 30px 100px;
            text-align: left;
        }

        #hero h1 {
            font-size: clamp(3rem, 8vw, 5.5rem);
            font-weight: 700;
            line-height: 1.1;
            margin-bottom: 30px;
            letter-spacing: -0.02em;
        }

        #hero h1 .highlight {
            color: var(--cyan-bright);
            display: block;
        }

        #hero .subtitle {
            font-size: clamp(1.1rem, 2.5vw, 1.4rem);
            color: var(--gray-light);
            max-width: 900px;
            margin-bottom: 20px;
            line-height: 1.7;
            font-weight: 400;
        }

        #hero .description {
            font-size: clamp(1rem, 2vw, 1.15rem);
            color: var(--gray-medium);
            max-width: 850px;
            line-height: 1.7;
            margin-bottom: 50px;
        }

        /* Buttons */
        .button-group {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
        }

        .btn {
            display: inline-block;
            padding: 16px 36px;
            font-size: 1rem;
            font-weight: 600;
            text-decoration: none;
            border-radius: 8px;
            transition: all 0.3s ease;
            cursor: pointer;
            border: none;
            font-family: 'Inter', sans-serif;
        }

        .btn-primary {
            background: var(--cyan);
            color: var(--bg-dark);
            box-shadow: 0 4px 14px rgba(0, 217, 200, 0.4);
        }

        .btn-primary:hover {
            background: var(--cyan-bright);
            box-shadow: 0 6px 20px rgba(0, 217, 200, 0.5);
            transform: translateY(-2px);
        }

        .btn-secondary {
            background: transparent;
            color: var(--cyan-bright);
            border: 2px solid var(--cyan);
        }

        .btn-secondary:hover {
            background: rgba(0, 217, 200, 0.1);
            transform: translateY(-2px);
        }

        /* About Section */
        #about {
            background: linear-gradient(135deg, var(--bg-base) 0%, var(--bg-light) 100%);
            padding: 120px 30px;
        }

        #about h2 {
            font-size: clamp(2.5rem, 5vw, 3.5rem);
            margin-bottom: 60px;
            font-weight: 700;
            letter-spacing: -0.02em;
        }

        .about-layout {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            align-items: start;
        }

        .about-text p {
            font-size: 1.15rem;
            line-height: 1.8;
            color: var(--gray-light);
            margin-bottom: 24px;
        }

        .features-grid {
            display: flex;
            flex-direction: column;
            gap: 16px;
        }

        .feature-item {
            padding: 24px;
            background: rgba(0, 217, 200, 0.05);
            border-left: 3px solid var(--cyan);
            border-radius: 4px;
            transition: all 0.3s ease;
            font-size: 1.05rem;
            font-weight: 500;
            color: var(--white);
        }

        .feature-item:hover {
            background: rgba(0, 217, 200, 0.1);
            border-left-color: var(--green-bright);
            transform: translateX(8px);
        }

        /* Co-Pilot Section */
        #copilot {
            padding: 120px 30px;
            background: linear-gradient(135deg, var(--bg-dark) 0%, var(--bg-base) 50%, rgba(0, 153, 255, 0.05) 100%);
        }

        #copilot h2 {
            font-size: clamp(2.5rem, 5vw, 3.5rem);
            margin-bottom: 24px;
            font-weight: 700;
            letter-spacing: -0.02em;
        }

        #copilot h2 .gradient-text {
            background: linear-gradient(135deg, var(--green) 0%, var(--cyan) 50%, var(--blue) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .copilot-intro {
            font-size: 1.2rem;
            color: var(--gray-light);
            max-width: 800px;
            margin: 0 auto 60px;
            text-align: center;
            line-height: 1.7;
        }

        .chat-interface {
            max-width: 800px;
            margin: 0 auto;
            background: rgba(15, 30, 46, 0.5);
            border: 1px solid rgba(0, 217, 200, 0.2);
            border-radius: 12px;
            padding: 36px;
            backdrop-filter: blur(10px);
        }

        .query-item {
            padding: 20px 24px;
            background: rgba(10, 22, 40, 0.6);
            border: 1px solid rgba(0, 217, 200, 0.2);
            border-radius: 8px;
            margin-bottom: 16px;
            font-size: 1.05rem;
            color: var(--gray-light);
            line-height: 1.6;
            transition: all 0.3s ease;
        }

        .query-item:hover {
            border-color: var(--cyan);
            background: rgba(0, 217, 200, 0.08);
            transform: translateX(4px);
        }

        .query-item::before {
            content: '"';
            color: var(--cyan-bright);
            font-weight: 700;
            font-size: 1.3rem;
            margin-right: 4px;
        }

        .query-item::after {
            content: '"';
            color: var(--cyan-bright);
            font-weight: 700;
            font-size: 1.3rem;
            margin-left: 4px;
        }

        .copilot-footer {
            margin-top: 32px;
            text-align: center;
            font-size: 0.95rem;
            color: var(--green-bright);
            font-weight: 600;
            letter-spacing: 0.05em;
        }

        /* Vision Section */
        #vision {
            padding: 120px 30px;
            background: linear-gradient(135deg, rgba(0, 255, 65, 0.03) 0%, var(--bg-base) 50%, rgba(0, 153, 255, 0.05) 100%);
        }

        #vision h2 {
            font-size: clamp(2.5rem, 5vw, 3.5rem);
            margin-bottom: 24px;
            text-align: center;
            font-weight: 700;
            letter-spacing: -0.02em;
        }

        .vision-subtitle {
            font-size: 1.3rem;
            color: var(--gray-light);
            text-align: center;
            margin-bottom: 70px;
        }

        .vision-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 32px;
            margin-bottom: 60px;
        }

        .vision-card {
            padding: 40px 32px;
            background: rgba(15, 30, 46, 0.4);
            border: 1px solid rgba(0, 217, 200, 0.2);
            border-radius: 12px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .vision-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, var(--green), var(--cyan), var(--blue));
            transform: scaleX(0);
            transform-origin: left;
            transition: transform 0.3s ease;
        }

        .vision-card:hover::before {
            transform: scaleX(1);
        }

        .vision-card:hover {
            transform: translateY(-8px);
            border-color: var(--cyan);
            box-shadow: 0 16px 40px rgba(0, 217, 200, 0.3);
        }

        .vision-card h3 {
            font-size: 1.5rem;
            margin-bottom: 16px;
            color: var(--cyan-bright);
            font-weight: 600;
        }

        .vision-card p {
            font-size: 1.05rem;
            line-height: 1.7;
            color: var(--gray-light);
        }

        .launch-notice {
            text-align: center;
            font-size: 1.15rem;
            color: var(--green-bright);
            font-weight: 600;
        }

        /* Contact Section */
        #contact {
            padding: 120px 30px;
            background: var(--bg-dark);
        }

        #contact h2 {
            font-size: clamp(2.5rem, 5vw, 3.5rem);
            margin-bottom: 24px;
            text-align: center;
            font-weight: 700;
            letter-spacing: -0.02em;
        }

        .contact-intro {
            font-size: 1.2rem;
            color: var(--gray-light);
            text-align: center;
            margin-bottom: 60px;
        }

        .contact-form {
            max-width: 650px;
            margin: 0 auto;
        }

        .form-group {
            margin-bottom: 28px;
        }

        .form-group label {
            display: block;
            margin-bottom: 10px;
            font-size: 0.95rem;
            font-weight: 600;
            color: var(--cyan-bright);
            letter-spacing: 0.02em;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 14px 16px;
            background: rgba(15, 30, 46, 0.4);
            border: 1px solid rgba(0, 217, 200, 0.3);
            border-radius: 8px;
            color: var(--white);
            font-size: 1rem;
            font-family: 'Inter', sans-serif;
            transition: all 0.3s ease;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--cyan);
            background: rgba(15, 30, 46, 0.6);
        }

        .form-group textarea {
            min-height: 140px;
            resize: vertical;
        }

        .form-submit {
            width: 100%;
        }

        /* Footer */
        #footer {
            padding: 70px 30px 50px;
            background: var(--bg-dark);
            border-top: 1px solid rgba(0, 217, 200, 0.2);
            text-align: center;
        }

        .footer-logo {
            margin: 0 auto 40px;
        }

        .footer-heading {
            font-size: 1.2rem;
            margin-bottom: 24px;
            color: var(--cyan-bright);
            font-weight: 600;
        }

        .social-links {
            display: flex;
            gap: 20px;
            justify-content: center;
            margin-bottom: 40px;
        }

        .social-links a {
            width: 50px;
            height: 50px;
            border: 2px solid var(--cyan);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--cyan-bright);
            text-decoration: none;
            font-size: 1.2rem;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .social-links a:hover {
            background: var(--cyan);
            color: var(--bg-dark);
            transform: translateY(-4px);
        }

        .footer-text {
            font-size: 0.9rem;
            color: var(--gray-medium);
        }

        /* Responsive */
        @media (max-width: 768px) {
            section {
                padding: 80px 20px;
            }

            #hero {
                padding: 100px 20px 80px;
                text-align: center;
            }

            .badge {
                font-size: 0.85rem;
                padding: 10px 20px;
            }

            .button-group {
                flex-direction: column;
                width: 100%;
            }

            .btn {
                width: 100%;
                text-align: center;
            }

            .about-layout {
                grid-template-columns: 1fr;
                gap: 50px;
            }

            .vision-grid {
                grid-template-columns: 1fr;
            }

            .chat-interface {
                padding: 24px;
            }

            .logo {
                margin: 0 auto 30px;
            }
        }

        /* Smooth Animations */
        .fade-in {
            opacity: 0;
            transform: translateY(30px);
            transition: opacity 0.8s ease, transform 0.8s ease;
        }

        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
</head>
<body>
    <!-- Hero Section -->
    <section id="hero">
        <div class="logo">
            <div class="logo-rays">
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
            </div>
            <div class="logo-center"></div>
        </div>

        <div class="badge">
            <span class="badge-dot"></span>
            Accelerating Vision 2030's AI Momentum
        </div>

        <h1>
            The Bloomberg Terminal for<br>Saudi Arabia's <span class="highlight">AI Ecosystem</span>
        </h1>

        <p class="subtitle">Unifying fragmented data into actionable intelligence.</p>
        
        <p class="description">The first platform connecting capital flows, policies, and partnerships across the Kingdom's $100B+ AI transformation.</p>

        <div class="button-group">
            <a href="#" class="btn btn-primary">Stay Informed</a>
            <a href="#contact" class="btn btn-secondary">Get in Touch</a>
        </div>
    </section>

    <!-- About Section -->
    <section id="about" class="fade-in">
        <h2>About Aqool Wire</h2>
        
        <div class="about-layout">
            <div class="about-text">
                <p>The Aqool Wire is an independent platform built for the private sector to understand and engage with Saudi Arabia's AI build-out.</p>
                
                <p>It integrates open government data — from SDAIA, DGA, and other sources — with real-time analysis to bring transparency and foresight to Vision 2030.</p>
            </div>

            <div class="features-grid">
                <div class="feature-item">Policy & Regulation Tracking</div>
                <div class="feature-item">Investment & Infrastructure Visibility</div>
                <div class="feature-item">Skills, Talent & AI Workforce Signals</div>
            </div>
        </div>
    </section>

    <!-- Co-Pilot Section -->
    <section id="copilot" class="fade-in">
        <h2>The <span class="gradient-text">Insight Co-Pilot</span></h2>
        
        <p class="copilot-intro">A bilingual AI assistant for investors, innovators, and policymakers — turning open Saudi data into real-time foresight.</p>

        <div class="chat-interface">
            <div class="query-item">What if SDAIA's compute capacity doubles by 2026?</div>
            <div class="query-item">Which Saudi regions are attracting the most AI startups this quarter?</div>
            <div class="query-item">How would AI investment flows shift if GPU imports are delayed?</div>
            <div class="query-item">Which sectors will see the highest AI talent demand next year?</div>
        </div>

        <p class="copilot-footer">Powered by open Saudi data + AI analysis</p>
    </section>

    <!-- Vision Section -->
    <section id="vision" class="fade-in">
        <h2>What's Coming</h2>
        <p class="vision-subtitle">Building the data layer of Saudi Arabia's AI future.</p>

        <div class="vision-grid">
            <div class="vision-card">
                <h3>AI Investment Observatory</h3>
                <p>Track deals, MOUs & programs shaping Saudi Arabia's AI economy in real-time.</p>
            </div>

            <div class="vision-card">
                <h3>Regulatory Intelligence</h3>
                <p>Live bilingual tracker of AI laws, standards, and policy developments.</p>
            </div>

            <div class="vision-card">
                <h3>Talent & Skills Signals</h3>
                <p>Map where AI skills are being trained, funded, and deployed across the Kingdom.</p>
            </div>
        </div>

        <p class="launch-notice">Launching 2026 — early partners welcome.</p>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="fade-in">
        <h2>Get in Touch</h2>
        <p class="contact-intro">Interested in sponsoring, partnering, or early access? Let's talk.</p>

        <form class="contact-form" id="contactForm">
            <div class="form-group">
                <label for="name">Name</label>
                <input type="text" id="name" name="name" required>
            </div>

            <div class="form-group">
                <label for="org">Organization</label>
                <input type="text" id="org" name="org" required>
            </div>

            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" required>
            </div>

            <div class="form-group">
                <label for="message">Message</label>
                <textarea id="message" name="message" required></textarea>
            </div>

            <button type="submit" class="btn btn-primary form-submit">Send Message</button>
        </form>
    </section>

    <!-- Footer -->
    <footer id="footer">
        <div class="logo footer-logo">
            <div class="logo-rays">
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
                <div class="logo-ray"></div>
            </div>
            <div class="logo-center"></div>
        </div>

        <p class="footer-heading">Follow the Wire</p>
        
        <div class="social-links">
            <a href="#" aria-label="LinkedIn">in</a>
            <a href="#" aria-label="X">𝕏</a>
        </div>

        <p class="footer-text">Copyright © 2025 Aqool LLC.</p>
    </footer>

    <script>
        // Smooth Scroll
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                const href = this.getAttribute('href');
                if (href !== '#') {
                    e.preventDefault();
                    const target = document.querySelector(href);
                    if (target) {
                        target.scrollIntoView({
                            behavior: 'smooth',
                            block: 'start'
                        });
                    }
                }
            });
        });

        // Scroll Reveal Animation
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -100px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.fade-in').forEach(element => {
            observer.observe(element);
        });

        // Form Handler
        document.getElementById('contactForm').addEventListener('submit', function(e) {
            e.preventDefault();
            alert('Thank you for your interest! We\'ll be in touch soon.');
            this.reset();
        });
    </script>
</body>
</html>